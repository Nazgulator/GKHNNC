
<Center>
    <h3>
        Программа VODA
    </h3>
    <h5>
        Проведение анализа данных по воде и отоплению.
    </h5>
    <h5>
        Выберите месяц для загрузки и анализа данных.
    </h5>

    <h3>
        Горячая вода
    </h3>
    <div class="container">
        <div class="row form-group">
            @for (int i = 0; i < ViewBag.Month.Count / 2; i++)
            {
                string cl1 = "btn btn-secondary";
                if (ViewBag.Go[i] < 4 && ViewBag.Go[i] > 0) { cl1 = "btn btn-warning"; }
                if (ViewBag.Go[i] == 4) { cl1 = "btn btn-success"; }
                <div class="col-md-2">

                    <button class="@cl1" onclick="location.href='@Url.Action("VODAMonth", "Home", new { Month = i+1 } )'">
                        @ViewBag.Month[i]
                    </button>

                </div>
            }
        </div>
        <div class="row  form-group">
            @for (int i = ViewBag.Month.Count / 2; i < ViewBag.Month.Count; i++)
            {
                string cl1 = "btn btn-secondary";
                if (ViewBag.Go[i] < 4 && ViewBag.Go[i] > 0) { cl1 = "btn btn-warning"; }
                if (ViewBag.Go[i] == 4) { cl1 = "btn btn-success"; }

                <div class="col-md-2">

                    <button class="@cl1" onclick="location.href='@Url.Action("VODAMonth", "Home", new { Month = i+1 } )'">
                        @ViewBag.Month[i]
                    </button>

                </div>
            }
        </div>


    </div>
    <h3> Отопление </h3>
    <div class="container">

        <div class="row form-group">
            @for (int i = 0; i < ViewBag.Month.Count / 2; i++)
            {
                string cl1 = "btn btn-secondary";
                if (ViewBag.Go[i] < 4 && ViewBag.Go[i] > 0) { cl1 = "btn btn-warning"; }
                if (ViewBag.Go[i] >= 4) { cl1 = "btn btn-success"; }
                <div class="col-md-2">

                    <button class="@cl1" onclick="location.href='@Url.Action("OtoplenieMonth", "Home", new { Month = i+1, Year = DateTime.Now.Year } )'">
                        @ViewBag.Month[i]
                    </button>

                </div>
            }
        </div>
        <div class="row  form-group">
            @for (int i = ViewBag.Month.Count / 2; i < ViewBag.Month.Count; i++)
            {
                string cl1 = "btn btn-secondary";
                if (ViewBag.Go[i] < 4 && ViewBag.Go[i] > 0) { cl1 = "btn btn-warning"; }
                if (ViewBag.Go[i] >= 4) { cl1 = "btn btn-success"; }

                <div class="col-md-2">

                    <button class="@cl1" onclick="location.href='@Url.Action("OtoplenieMonth", "Home", new { Month = i + 1, Year = DateTime.Now.Year } )'">
                        @ViewBag.Month[i]
                    </button>

                </div>
            }
        </div>


    </div>
    <h3> Отчеты </h3>
    <div class="container">

        <div class="row form-group">
            @for (int i = 0; i < ViewBag.Month.Count / 2; i++)
            {
                string cl1 = "btn btn-secondary";
                if (ViewBag.Go[i] < 4 && ViewBag.Go[i] > 0) { cl1 = "btn btn-warning"; }
                if (ViewBag.Go[i] == 4&& ViewBag.Arenda[i]==true) { cl1 = "btn btn-success"; }
                <div class="col-md-2">

                    <button class="@cl1" onclick="location.href='@Url.Action("OtchetMonth", "Home", new { Month = i+1, Year = DateTime.Now.Year } )'">
                        @ViewBag.Month[i]
                    </button>

                </div>
            }
        </div>
        <div class="row  form-group">
            @for (int i = ViewBag.Month.Count / 2; i < ViewBag.Month.Count; i++)
            {
                string cl1 = "btn btn-secondary";
                if (ViewBag.Go[i] < 4 && ViewBag.Go[i] > 0) { cl1 = "btn btn-warning"; }
                if (ViewBag.Go[i] == 4) { cl1 = "btn btn-success"; }

                <div class="col-md-2">

                    <button class="@cl1" onclick="location.href='@Url.Action("OtchetMonth", "Home", new { Month = i + 1, Year = DateTime.Now.Year } )'">
                        @ViewBag.Month[i]
                    </button>

                </div>
            }
        </div>


    </div>
    </br>
    <p>
        <button class="btn btn-success btn-lg" style="width:500Px" onclick="location.href='@Url.Action("Index", "Negilayas")'">
            <i class="glyphicon glyphicon-file"></i> Коды ОБСД арендаторов (кварсис,соран,эол) &raquo;
        </button>
    </p>
    <p>
        <button class="btn btn-success btn-lg" style="width:500Px" onclick="location.href='@Url.Action("Index", "SVNs")'">
            <i class="glyphicon glyphicon-file"></i> Все СВН записи &raquo;
        </button>
    </p>
    <p>
        <button class="btn btn-success btn-lg" style="width:500Px" onclick="location.href='@Url.Action("Index", "UEVs")'">
            <i class="glyphicon glyphicon-list-alt"></i> Все УЭВ записи &raquo;
        </button>
    </p>
    <p>
        <button class="btn btn-success btn-lg" style="width:500Px" onclick="location.href='@Url.Action("Index", "OBSDs")'">
            <i class="glyphicon glyphicon-list-alt"></i> Все ОБСД записи &raquo;
        </button>
    </p>
    <p>
        <button class="btn btn-success btn-lg" style="width:500Px" onclick="location.href='@Url.Action("Index", "IPUs")'">
            <i class="glyphicon glyphicon-list-alt"></i> Все ИПУ записи &raquo;
        </button>
    </p>
    <p>
        <button class="btn btn-success btn-lg" style="width:500Px" onclick="location.href='@Url.Action("Index", "OPUs")'">
            <i class="glyphicon glyphicon-list-alt"></i> Все ОПУ записи &raquo;
        </button>
    </p>

</Center>
@section Scripts {

    @Scripts.Render("~/scripts/jquery-3.3.1.min.js")
    @Scripts.Render("~/scripts/jquery-ui-1.12.1.min.js")
    @Scripts.Render("~/scripts/nprogress.min.js")
    @Scripts.Render("~/scripts/jquery.signalR-2.4.0.min.js")
    @Scripts.Render("~/signalr/hubs")

    <script>
        $(function () {
            var PB = $.connection.progressHub;
            console.log("Loading...");
            PB.client.sendMessage = function (message, count) {
                // update progress
                console.log(count);
                UpdateProgress(message, count);
                //alert(message);
            };

            $.connection.hub.start().done(function () {
                console.log("Приконнектились");
                // call the method CallLongOperation defined in the Hub
                PB.server.getCountAndMessage();
                $("#progressBar").hide();
            });


            // Update the progress bar
            function UpdateProgress(message, count) {
                $("#progressBar").fadeIn("slow");
                var result = $("#result");
                if (count >= 98) { $("#progressBar").fadeOut("slow"); }
                result.html(message);

                //$("#progressBar").data("progressbar").value(count);

                $("#progressBar").attr("aria-valuenow", count);
                $("#progressBar").width(count + '%');
                $("#progressBar").text(count + '%');
                $("#nadpis").text(message);
                // $("#progressBar").css("width", count);
            }
        });
    </script>
    
}